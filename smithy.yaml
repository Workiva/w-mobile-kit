language: python

runner_image: drydock-prod.workiva.net/workiva/smithy-runner:112206
before_install:
  - bundle install --path ~/.gem
  - rbenv rehash

script:
  - ./smithy.sh

artifacts:
  - code_coverage/*.zip
  - test_reports/*.xml

test_reports:
  - test-reports/*.xml
#  - code_coverage/*.html
