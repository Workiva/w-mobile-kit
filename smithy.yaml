language: python

before_install:
  - gem install --install-dir ~/.gem ocunit2junit

script:
  - ./smithy.sh

artifacts:
  - dist/**/*.ipa
  - dist/**/*.tar.gz
  # - coverage/**/*  ## We do not generate for iOS atm, and this is including doc viewers stuff.
  - test_reports/*.xml

test_reports:
  - test-reports/*.xml
